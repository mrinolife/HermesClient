name: HermesClient
options:
  bundleIdPrefix: com.jznle
  deploymentTarget:
    iOS: "18.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "6.0"
    TARGETED_DEVICE_FAMILY: "1"
    INFOPLIST_FILE: HermesClient/Info.plist
    DEVELOPMENT_TEAM: ""

targets:
  HermesClient:
    type: application
    platform: iOS
    sources:
      - path: HermesClient
      - path: InaAnimation
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.jznle.hermesclient
        INFOPLIST_FILE: HermesClient/Info.plist
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        SWIFT_EMIT_LOC_STRINGS: "NO"
    preBuildScripts:
      - name: Inject Build Info
        script: |
          echo "BUILD_DATE=$(date +%Y-%m-%d)" > /tmp/build_env.txt
          echo "COMMIT_HASH=${GITHUB_SHA:-local}" >> /tmp/build_env.txt
